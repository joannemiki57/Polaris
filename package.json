{
  "name": "mindgraph-openalex",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,magenta \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server && cp -r client/dist server/dist/public",
    "start": "node server/dist/index.js",
    "session:changelog": "bash scripts/session-changelog-hook.sh",
    "hooks:install": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
