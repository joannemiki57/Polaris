---
tags: [repo, vault]
---

# Repo layout — code vs vault

- **Vault** (Obsidian): the folder **`Cursor Hack/`** containing this note and `.obsidian/`.
- **Code** (Node monorepo): parent directory **`Cursor Hackaton/`** with `client/`, `server/`, `package.json`.

Obsidian only indexes files **inside** the vault folder by default, so `client/` and `server/` do not appear as notes unless you add separate links or use a symlink (not required).

**Hub**: [[00 · Hub - MindGraph project]]
